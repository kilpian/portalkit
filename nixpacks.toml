[phases.setup]
nixPkgs = ['nodejs_24', 'dejavu_fonts', 'fontconfig', 'python311', 'python311Packages.pip', 'cairo', 'pango', 'pkg-config']
cmds = ['pip3 install manim --break-system-packages --quiet 2>&1 | tail -3 || echo "pip install attempted"']

[phases.build]
cmds = ['fc-cache -fv', 'npm install --production=false']
